# ESLint and Prettier Preset

This preset will install and configure ESLint and Prettier in your project.

## Usage

Run the following from the root of your project:

```
preset apply springloadedco/presets --path eslint-vue
```

