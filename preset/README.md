# Initialize Springloaded Preset

This preset can be used to create a new preset.

## Usage

Run the following from the root of the Springloaded preset repo:

```
preset apply springloadedco/presets --path preset
```
